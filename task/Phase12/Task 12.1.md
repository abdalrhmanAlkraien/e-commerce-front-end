Create site-wide navigation header.

Create src/components/layout/Header.tsx:
1. Logo/Brand name links to home
2. Navigation links: Products, Categories
3. Cart icon with item count badge
4. User menu dropdown (if authenticated):
    - View Profile
    - Orders
    - Logout
5. Login/Register buttons (if not authenticated)
6. Mobile responsive (hamburger menu)
7. Search bar in header (bonus)

Add to root layout